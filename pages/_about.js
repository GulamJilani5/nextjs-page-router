
// We can add 'about' page inside 'pages' folder directly and It will be considerd as nested page after 
// 'pages' folder(our domain). It is same as creating 'about' page inside 'pages' folder and then 
// creating index.js file.

export default function AboutPage() {
  return (
    <div>
        This is an about 
    </div>
  )
}
